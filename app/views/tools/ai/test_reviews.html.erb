<% content_for :title, t("ai.recent_test_reviews") %>

<div class="level mb-4">
  <div class="level-left">
    <div>
      <nav class="breadcrumb is-small"><ul>
        <li><%= link_to t("ai.reports_title"), tools_ai_path %></li>
        <li class="is-active"><a><%= t("ai.recent_test_reviews") %></a></li>
      </ul></nav>
      <h1 class="fw-bold mb-1">🧪 <%= t("ai.recent_test_reviews") %></h1>
      <p class="text-muted fs-7"><%= t("ai.recent_test_reviews_subtitle") %></p>
    </div>
  </div>
</div>

<%= render "tools/ai/reviews_table", reviews: @reviews %>

<div class="mt-3"><%= paginate @reviews if respond_to?(:paginate) %></div>
