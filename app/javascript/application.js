// Entry point for the build script in your package.json
import "@hotwired/turbo-rails"
import "./controllers"

// Bootstrap 5
import "bootstrap"

// Charts
import "chartkick/chart.js"

// Trix WYSIWYG editor (registers <trix-editor> custom element)
import "trix"
